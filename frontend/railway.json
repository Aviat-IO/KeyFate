{
	"$schema": "https://railway.com/railway.schema.json",
	"build": {
		"builder": "DOCKERFILE",
		"dockerfilePath": "Dockerfile"
	},
	"deploy": {
		"preDeployCommand": ["bun run db:migrate:production"],
		"healthcheckPath": "/api/health/ready",
		"healthcheckTimeout": 300
	}
}
